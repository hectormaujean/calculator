import { Box } from '@mui/material';

export function ScreenCurrentOperation() {
    return (
        <Box
            sx={{
                paddingTop: 5,
                marginRight: 2,
                textAlign: 'right',
            }}
        >
            50 × 200
        </Box>
    );
}
